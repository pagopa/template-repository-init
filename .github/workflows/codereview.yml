name: 🔎 TEMPLATE-PAYMENTS - Code Review

on:
  push:
    branches: [ main, uat, develop ]
  pull_request:
    types: [ opened, edited, synchronize ]
    branches: [ main, uat, develop ]

env:
  JAVA_VERSION: '21'
  JAVA_DISTRIBUTION: 'corretto'

jobs:
  code-review:
    name: Code Review & Quality Analysis
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@692973e3d937129bcbf40652eb9f2f61becf3332 # v4.1.7
        with:
          fetch-depth: 0
          
      - name: Setup Java
        uses: actions/setup-java@99b8673ff64fbf99d8d325f52d9a5bdedb8483e9 # v4.2.1
        with:
          distribution: ${{ env.JAVA_DISTRIBUTION }}
          java-version: ${{ env.JAVA_VERSION }}
          cache: 'gradle'

      - name: Cache Gradle packages
        uses: actions/cache@ef2de19b1145563b7e615d9edce6a9f96396c5ca # v3.3.2
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Grant execute permission for gradlew
        run: chmod +x ./gradlew

      - name: Run Tests & Coverage
        id: tests
        run: ./gradlew clean build jacocoTestReport --info

      - name: SonarCloud Analysis
        id: sonar
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: |
          ./gradlew sonar \
          -Dorg.gradle.jvmargs=-Xmx4096M \
          -Dsonar.host.url=https://sonarcloud.io \
          -Dsonar.organization=${{ vars.SONARCLOUD_ORG }} \
          -Dsonar.projectKey=${{ vars.SONARCLOUD_PROJECT_KEY }} \
          -Dsonar.projectName="${{ vars.SONARCLOUD_PROJECT_NAME }}" \
          -Dsonar.sources=src/main \
          -Dsonar.tests=src/test \
          -Dsonar.coverage.jacoco.xmlReportPaths=build/reports/jacoco/test/jacocoTestReport.xml \
          -Dsonar.exclusions='**/enums/**, **/model/**, **/dto/**, **/*Constant*, **/*Config.java, **/*Scheduler.java, **/*Application.java, **/src/test/**, **/Dummy*.java' \
          -Dsonar.qualitygate.wait=true

      - name: Save Test Results
        if: always()
        uses: actions/upload-artifact@1eb3cb2b3e0f29609092a73eb181e0ba64d90c7a # v4.1.0
        with:
          name: test-results
          path: |
            build/reports/tests/
            build/reports/jacoco/
          retention-days: 7

      - name: Create Code Review Summary
        if: always()
        run: |
          echo "## Code Review Summary" >> $GITHUB_STEP_SUMMARY
          echo "### Test Results Location" >> $GITHUB_STEP_SUMMARY
          echo "Test reports are available in the artifacts section" >> $GITHUB_STEP_SUMMARY
          echo "### Sonar Analysis" >> $GITHUB_STEP_SUMMARY
          echo "Check SonarCloud dashboard for detailed analysis" >> $GITHUB_STEP_SUMMARY